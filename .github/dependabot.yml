# Keeps SHA-pinned actions from rotting: Dependabot PRs bump the pinned
# commit (and the version comment) when actions release updates.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
